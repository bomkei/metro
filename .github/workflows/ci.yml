name: CI

on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    container: silkeh/clang:latest
    steps:
    - uses: actions/checkout@v3
    - run: make -j
      env:
        TERM: xterm-256color
